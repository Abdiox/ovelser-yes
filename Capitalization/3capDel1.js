"use strict";

// const navn = "peter";

// const navnStart = navn.indexOf(0);

// const navnSlut = navn.length;

// const navnStortT = navn.substring(0, 2).toLowerCase() + navn[2].toUpperCase() + navn.substring(3, 5).toLowerCase();

// console.log(navnStortT);

const name = "peter";

const stortP = name[0].toUpperCase() + name.substring(1, 5).toLowerCase();

console.log(stortP);

const name1 = "PETER";

const stortP1 = name1[0].toUpperCase() + name1.substring(1, 5).toLowerCase();

console.log(stortP1);

const name2 = "pEtEr";

const stortP2 = name2[0].toUpperCase() + name2.substring(1, 5).toLowerCase();

console.log(stortP2);
